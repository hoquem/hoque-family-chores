name: hoque_family_chores
description: "A Family Chores Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+27

environment:
  sdk: ^3.7.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2 # Check pub.dev for the latest version
  provider: ^6.1.5 # Check for the latest version on pub.dev
  flutter_dotenv: ^5.2.1 # Check for latest version
  uuid: ^4.5.1
  logger: ^2.5.0
  rxdart: ^0.27.7 # For reactive programming support
  get_it: ^7.6.0
  collection: ^1.17.0
  meta: ^1.10.0 # For annotations like @protected
  http: ^1.2.0 # For HTTP requests
  equatable: ^2.0.5 # For value equality
  dartz: ^0.10.1 # For functional programming (Either, Option)
  shared_preferences: ^2.2.2 # For settings persistence

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  firebase_core: ^3.13.0 # Check pub.dev for the latest version
  firebase_auth: ^5.5.3 # Check pub.dev for the latest version
  cloud_firestore: ^5.6.7 # Check pub.dev for the latest version
  firebase_storage: ^12.3.9 # For photo uploads
  sign_in_with_apple: ^6.1.4 # OAuth: Sign in with Apple (Phase 1)
  google_sign_in: ^6.2.1 # OAuth: Google sign-in (Phase 1)
  crypto: ^3.0.3 # Apple sign-in nonce hashing
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  hooks_riverpod: ^2.4.0
  go_router: ^13.0.0
  freezed_annotation: ^3.0.0
  connectivity_plus: ^7.0.0
  flutter_image_compress: ^2.3.0 # For image compression
  firebase_messaging: ^15.2.0 # For push notifications
  flutter_local_notifications: ^16.3.0 # For local notifications
  timezone: ^0.9.4 # For scheduled notifications
  app_settings: ^5.2.0 # For app settings
  cached_network_image: ^3.3.1 # For image caching
  firebase_crashlytics: ^4.3.2 # For crash reporting
  image_picker: ^1.2.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3 # Check pub.dev for the latest version
  mocktail: ^1.0.4
  mockito: ^5.4.0
  bdd_widget_test: ^1.7.0 # BDD testing with Gherkin syntax
    
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  riverpod_generator: ^2.6.5
  build_runner: ^2.5.4
  freezed: ^3.0.0
  change_app_package_name: ^1.5.0
  mocktail_image_network: ^1.3.0

# Source of truth for these PNGs is the sibling .svg files; regenerate with
#   rsvg-convert -w 1024 -h 1024 -b '#FB8C00' assets/icon/app_icon.svg -o assets/icon/app_icon.png
# app_icon.png must stay full-bleed and opaque (no alpha, no pre-applied
# corners) - every platform applies its own mask.
flutter_launcher_icons:
    android: "launcher_icon"
    ios: true
    web:
      generate: true
      image_path: "assets/icon/app_icon.png"
    macos:
      generate: true
      image_path: "assets/icon/app_icon.png"
    windows:
      generate: true
      image_path: "assets/icon/app_icon.png"
    image_path: "assets/icon/app_icon.png"
    # Android adaptive icons: the launcher masks and parallaxes these two
    # layers independently, so the foreground is inset into the safe zone.
    adaptive_icon_background: "assets/icon/app_icon_background.png"
    adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
